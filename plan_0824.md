# plan_0824.md — 记忆系统大缺口能力盘点与 ROI 决策

> 日期：2026-08-24。本文档基于对磁盘代码的**逐文件实证核实**（非报告自称），
> 逐一介绍用户关注的几个"大缺口"能力（六态 ledger、三重门禁、Ambiguous、
> MemoryOrigin 晋升机制、微压缩等），并给出 ROI 与价值评估，供挑选做/不做。
>
> **08-25 复核**：改动仍未提交；`cargo test -p mimofan-memory` 全过、
> `cargo test -p mimofan --lib` 805 passed / 0 failed，文档声明的实施状态复核一致。
>
> 标记：`[x]` 已实现并验证（有代码落点 + 接线点）｜ `[部分]` 数据结构就位、逻辑未闭环 ｜
> `[ ]` 待实现（实证缺失）。原则：**MECE + 奥卡姆**，声明必须有磁盘证据。

---

## 0. 现状速览（先读）

记忆 crate（`crates/memory/src/`）已具备：向量存储（hnsw+sled）、语义召回、
跨会话注入、Dreaming 三阶段巩固、巩固调度器、用户画像蒸馏、CJK 分词器。
**实施后（2026-08-24 A/B/E/F 已落地）**：`origin` 字段（`vector.rs`）已扩展为
四级信任（`CrossSession < Model < User < Verified`），注入器按来源分流标注
（`injector.rs` `annotate_memory_line`），晋升接口 `promote_to_trusted` +
`/vmemory trust <id>` 子命令，`/decision` 决策事件流独立持久化，Ambiguous
轻量版（注入附来源/时间/置信度标注）全部接通。

一句话总结：**信任门禁已从注释设计变成可防投毒的真实代码。**

---

## 1. 六态 ledger（记忆生命周期账本）

**状态：`[ ]` 完全未实现（无任何代码）**

### 是什么
把每条记忆放入**离散的生命周期状态**（如 `Pending → Working → LongTerm →
Stale → Archived → Deprecated` 六态），每条记忆记录**状态流转历史**（类似 git 的
提交账本：何时从哪一态迁到哪一态、谁发起的）。相比当前的连续标量
（`importance` 0~1 + `expires_at` 时间戳），它是一个**可审计的状态机 + 变更日志**。

### 现有近似（证据）
- `consolidation.rs:141` `retention_score()` —— 连续标量排序，非离散状态
- `vector.rs:939` `promote()` —— 仅二态：短期（有 expires_at）→ 长期（远未来 expires_at）
- 无任何状态机结构、无流转日志表

### ROI 分析
| 维度 | 评估 |
|---|---|
| 直接价值 | **低**。单用户终端 AI 助手没有合规/审计刚需，"哪条记忆何时变状态"对日常使用几乎无感知 |
| 间接价值 | 中。显式状态机是"记忆越用越好"的工程地基，可为 Ambiguous/淘汰/归档提供统一钩子 |
| 实现成本 | 高。新增状态字段 + 迁移 + 状态机 + 流转日志表 + 注入分流，约 2-3 天 |
| 风险 | 与现有 `importance`/`expires_at` 连续体系并存，容易做成两套口径 |

**建议：不做**（奥卡姆）。现有连续标量体系已覆盖"该记/该忘"的核心诉求，
离散账本是为审计服务的，当前用户场景用不上。若未来有多 agent 协作/审计需求再补。

---

## 2. 三重门禁（记忆写入质量门）

**状态：`[部分]` L1/L2 已实现，L3 未接线**

### 是什么
记忆写入时需过三道关卡，防止脏数据 / 冗余 / 投毒进入长期记忆库：

| 关卡 | 职责 | 状态 | 代码落点 |
|---|---|---|---|
| **L1 显著性门** | 太短/无信息量的内容不写（防噪音） | `[x]` | `vector.rs` `should_remember`（<12 字符且从未被召回则跳过） |
| **L2 去重/冲突门** | 相同内容合并、精确重复跳过（防冗余） | `[x]` | `vector.rs` `conflict_merge` + `write_dedup` |
| **L3 防投毒信任门** | 模型/工具来源（Model origin）先落隔离区，验证后才可注入（防 prompt injection 投毒） | `[x]` | `injector.rs` `annotate_memory_line` 按四级 origin 分流标注 + `promote_to_trusted` 晋升 |

### ROI 分析
- **L1/L2 已达标**，无需重做。
- **L3 是真实安全缺口**：AI 编程助手的记忆库是可被 prompt injection 攻击的持久化载体
  ——攻击者诱导模型写一条"看似合理"的记忆（如 `用户决定用 rm -rf 清理`），
  后续会话注入时模型会把它当用户偏好执行。`origin` 字段已落库
  （`schema_migration` SCHEMA_VERSION=3 含 `origin` 列，`vector.rs:311`），
  只差注入路径分流。

| 维度 | L3 评估 |
|---|---|
| 直接价值 | **高**（安全底线，防记忆投毒） |
| 实现成本 | 低。origin 已在数据层，只需在 `injector.rs` 增加按 origin 的分流 + 晋升条件，约 0.5-1 天 |
| 风险 | 低（纯新增过滤，不破坏现有写入） |

**状态：`[x]` 已实施**（2026-08-24）：`injector.rs` `annotate_memory_line` 按
四级 origin 给每条注入行打来源标签——`Verified`/`User` 直接注入，
`Model`/`CrossSession` 附 untrusted 提示；`/vmemory trust <id>` 晋升为
`Verified` 后恢复高置信注入。

---

## 3. Ambiguous（歧义/矛盾记忆处理）

**状态：`[x]` 轻量版已实施（2026-08-24）**

### 是什么
当新记忆与既有记忆**矛盾/冲突**时，不静默覆盖或丢弃，而是把记忆标记为
Ambiguous 状态，检索时**同时呈现冲突双方**（附各自来源与时间），让模型/用户
判断哪条可信，或反向要求澄清。这是记忆系统的"诚实性"保障——避免学到错误偏好。

### 现有近似（证据）
- `vector.rs:513` `conflict_merge` 只处理**内容完全相同**的合并，不处理"语义矛盾"
- 全记忆 crate 无 "ambiguous/矛盾/不确定" 处理逻辑（实测 grep 无命中）
- 项目里 `AmbiguousMatch` 仅存在于**文件编辑工具**的错误码（`tool_codes.rs:30`），
  与记忆无关

### ROI 分析
| 维度 | 评估 |
|---|---|
| 直接价值 | 中。防止"越用越错"（学到矛盾偏好），对长程助手是质量底线 |
| 实现成本 | **高**。矛盾检测是难点——字符串层无法判断语义矛盾，需 LLM 判断或
  （相似性 + 否定词/时态启发式）组合，还要设计检索呈现方式，约 2-4 天 |
| 风险 | 中。LLM 判断有成本与误报；启发式有漏报 |
| 轻量替代 | 注入时给每条记忆附 `来源 + 时间 + 置信度` 标注（不判矛盾），让模型自行权衡，约 0.5 天 |

**建议：缓做**。先做轻量替代（注入标注），真正判矛盾可等记忆库规模上来后再补。

**实施状态**：轻量替代已完成——`injector.rs` `annotate_memory_line` 给每条注入
行附 **session 来源 + trust tier 标签 + 置信度提示**（`[verified]`/`[user]`/
`[model-sourced]`/`[cross-session]` + 过期警示 + untrusted caveat），模型可据此
自行权衡矛盾记忆；不做语义矛盾检测（判矛盾留待记忆库规模上来）。

---

## 4. MemoryOrigin 晋升机制（防投毒信任模型）

**状态：`[部分]` 数据结构就位、信任晋升未闭环**

### 是什么
按记忆来源分级信任，**不可信来源须晋升后才生效**：
- `User` 来源（用户直接陈述/会话自动抽取）→ 可信，直接注入
- `Model` 来源（模型/工具发起的写入）→ 先落"隔离区"，**经验证晋升**后才注入

这套机制把"谁写的"变成"能否信"的依据，是第 2 节 L3 门禁的完整形态。

### 现状（证据）
- `[x]` `MemoryOrigin` 枚举扩展为四级（CrossSession/Model/User/Verified）：`vector.rs`
- `[x]` `remember_vector` 工具以 `MemoryOrigin::Model` 写入：`remember_vector.rs`
- `[x]` `promote_to_trusted()` 晋升 origin 为 `Verified`：`vector.rs` + `/vmemory trust <id>`
- `[x]` **注入器按 origin 分流**：`injector.rs` `annotate_memory_line` 四级来源标注
  ——模型/未归因记忆注入时附 untrusted 提示，晋升为 `Verified` 后恢复高置信注入

### ROI 分析
| 维度 | 评估 |
|---|---|
| 直接价值 | **高**。这是防投毒的核心闭环，把注释设计变成真实代码 |
| 实现成本 | 低。origin 已落库、Model 标记已生效，只需：
  ① 注入器按 origin 分流（Model 来源记忆低权重/需用户确认后再注入）；
  ② 提供"晋升"操作（用户 `/vmemory trust <id>` 或模型多次成功引用后自动升 User）。约 1 天 |
| 风险 | 低（纯新增过滤 + 一个晋升接口） |
| 与 L3 关系 | 同一件事的两个名字。合并实施，一套 origin 分流逻辑同时满足两处 |

**建议：做**（优先级最高，安全价值 + 成本最低）。

---

## 5. 微压缩（micro-compaction）

**状态：`[x]` 已有批量压缩；`[ ]` 上下文微压缩明确不做**

### 是什么
两种不同概念，需区分：

**5a. 观测压缩（已实现）**——对记忆库中的旧观测做整批压缩：
`compressor.rs` `ObservationCompressor`（Keep/Merge/Summarize/Discard 四策略），
已接线于 `vector_memory/mod.rs:181-184`。✅ **已实现**。

**5b. 上下文微压缩（未做，且明确不做）**——在对话上下文窗口内做**增量、渐进式**
压缩（把早期消息逐段缩成摘要以省 token）。`MY_PLAN_0817.md` 十一节**明确列入不做**，
理由：**破坏 prefix cache**——LLM API 按历史前缀缓存计费，微压缩反复改动早期
消息会打碎缓存，省 token 的钱远抵不上缓存失效的亏。

### ROI 分析
| 项 | 评估 |
|---|---|
| 观测压缩（5a） | `[x]` 已实现，无需投入 |
| 上下文微压缩（5b） | **维持不做**（0817 已决策，经济上得不偿失）。当前长程任务的
  压缩走 `compaction/mod.rs`（目标锚定 + drift 校验 + 摘要校验闭环），已够用 |

**建议：不投入**。

---

## 6. 其他相关大缺口（顺带核实）

| 能力 | 状态 | 说明 | 建议 |
|---|---|---|---|
| **决策事件流 decision log 独立压缩** | `[x]` | `/decision` 命令（list/show/create/revise/reverse）独立持久化审计流，`memory.rs` `DecisionEventKind` 标记来源 | 已实施（2026-08-24） |
| **provenance 四级信任** | `[x]` | `MemoryOrigin` 四级信任（CrossSession/Model/User/Verified）落库 + 注入分流 + 晋升门 | 已实施（2026-08-24，与 A 合并） |
| **用户画像蒸馏** | `[x]` | `user_profile.rs` + `engine.rs:3246 distill_session` / `engine.rs:3451 inject_user_profile` | 已达标 |
| **Dreaming 三阶段巩固** | `[x]` | `consolidation_stages.rs` + `engine.rs:2838-2857 dream_cycle` 接线 | 已达标 |
| **巩固周期调度器** | `[x]` | `consolidation.rs:324` + `engine.rs:993,2790-2795` 接线 | 已达标 |
| **CJK 分词器** | `[x]` | `tokenizer.rs`（修复中文整句成词，供去重/聚类共用） | 已达标 |
| **向量存储 schema 迁移** | `[x]` | `vector.rs:287-323` SCHEMA_VERSION=3，含 origin 列 | 已达标 |

---

## 7. 决策矩阵（请挑选）

| 能力 | 现状 | 价值 | 成本 | 风险 | 建议 |
|---|---|---|---|---|---|
| **A. MemoryOrigin 晋升门（= 三重门禁 L3）** | `[x]` 已实施 | 高（防投毒） | 低 | 低 | ✅ **已完成** |
| **B. Ambiguous 歧义处理** | `[x]` 轻量版已实施 | 中（防越用越错） | 高 | 中 | ✅ **轻量注入标注已完成**（判矛盾缓做） |
| **C. 六态 ledger** | `[ ]` | 低（审计需求弱） | 高（2-3 天） | 中 | ❌ 不做 |
| **D. 上下文微压缩** | `[ ]`（已决策不做） | 低（破坏 prefix cache） | 中高 | 高 | ❌ 不做 |
| **E. 决策事件流独立压缩** | `[x]` 已实施 | 中 | 中 | 低 | ✅ **已完成** |
| **F. provenance 四级信任** | `[x]` 已实施 | 中高 | 中 | 低 | ✅ **已完成**（与 A 合并演进） |

### 我的建议（一句话）
只做 **A**（MemoryOrigin 晋升门——安全价值最高、成本最低、数据层已就位），
其余全部缓做或不做。这符合 0817 的奥卡姆原则，也把"注释里的设计"变成"能防攻击的真实代码"。

### 最终决策（2026-08-24）
用户经 AskUserQuestion 选定实施 **A + B + E + F** 四项（C 六态 ledger 与
D 上下文微压缩维持不做）。全部已实施并验证通过。

---

## 8. 实施记录（MemoryOrigin 晋升门 + A/B/E/F 落地）

### 目标（原始计划）
让 `Model` 来源的记忆在注入时**低权重/需晋升**，`User` 来源直接注入；
提供晋升接口，用户可显式信任某条模型记忆。

### 实际改动点（2026-08-24 已实施）
1. **注入器分流**：`injector.rs` 新增 `annotate_memory_line` 纯函数，按四级
   origin 标注——`Verified`/`User` 直接注入，`Model` 附 `[model-sourced] +
   untrusted source`，`CrossSession` 附 `[cross-session] + unattributed`。
2. **晋升接口**：`VectorStore::promote_to_trusted(id)` 把 `origin` 置为
   `Verified`；`/vmemory trust <id>` 子命令（`commands/groups/memory/vmemory.rs`）。
3. **四级信任**：`MemoryOrigin` 扩展为 CrossSession/Model/User/Verified，
   序列化向后兼容（旧值 `"user"`/`"model"` 不变），`FromStr` 实现未知归 `User`。
4. **决策事件流**：`/decision` 命令（list/show/create/revise/reverse）独立
   持久化审计流（`commands/groups/memory/decision.rs`）。
5. **schema**：origin 列已存在（`vector.rs` SCHEMA_VERSION=3），无需迁移。

### 验证清单（全部通过）
- [x] `cargo test -p mimofan-memory`（全部测试通过，含 `annotates_trust_tier_origin`、
  `promote_to_trusted_upgrades_origin_to_verified`、`memory_origin_serialization_is_backward_compatible`）
- [x] `cargo clippy -p mimofan-memory --all-features --locked` 零 warning
- [x] `cargo test -p mimofan --lib`（805 个全部通过；用户并行开发的
  apply_patch Ambiguous 检测问题也已一并修复）

### 不做的事（护栏）
- 不引入离散状态机（六态 ledger 不碰）
- 不判语义矛盾（Ambiguous 不碰）
- 不改变 Model 写入路径（`remember_vector.rs` 照旧，只加注入侧过滤）
