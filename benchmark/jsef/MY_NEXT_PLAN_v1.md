# 新增漏洞教学类别建议 v1

您好！基于您现有已覆盖35+种漏洞的优秀安全教学框架，从完善攻防教学体系、覆盖更多现代Web攻击场景的角度，我为您梳理了以下建议新增的漏洞类别。这些类别可以帮助学习者更全面地理解“深度防御”和“安全设计”的理念。

---

## 1. 不安全的设计与业务逻辑漏洞 (Insecure Design & Business Logic Vulnerabilities)

这类漏洞并非来自具体代码实现错误，而是源于设计阶段的缺陷，能绕过正常的业务流程。它们极具教学价值，能培养学习者的“攻击者思维”。

- **建议1：价格篡改 (Price Tampering)**
  - **漏洞描述**: 在电商或支付场景中，攻击者通过修改前端发送的商品价格或数量参数，以远低于正常价的价格完成购买。
  - **教学价值**: 强调“所有来自客户端的数据都不可信”，服务端必须对核心业务数据（如价格、权限、状态）进行独立的校验和计算。
  - **实现场景**: 创建一个简单的购物下单接口，其中商品价格是从客户端POST请求中直接获取并用于计算总价，而不是从服务端数据库中查询。安全版本则会从服务端获取价格。

- **建议2：关键业务步骤绕过 (Workflow Bypass)**
  - **漏洞描述**: 攻击者通过直接访问后续步骤的URL，绕过前置的验证、支付或审核流程。
  - **教学价值**: 教育开发者在设计多步流程时，每一步都必须验证上一步的状态是否已完成，而不是仅依赖前端的线性引导。
  - **实现场景**: 设计一个“下单 -> 支付 -> 查看订单详情”的流程。漏洞版本允许用户在未完成支付的情况下，通过直接访问 `/order/details?id=...` 查看到订单详情。安全版本则会在“查看订单详情”接口中校验该订单的支付状态。

---

## 2. API安全专项 (API Security)

随着前后端分离和微服务架构的普及，API安全变得至关重要。

- **建议1：批量赋值/过度的数据暴露 (Mass Assignment / Excessive Data Exposure)**
  - **漏洞描述**: API端点在接收和处理JSON对象时，直接将其反序列化为整个数据模型（Model/Entity），导致攻击者可以修改或查看其本无权访问的字段（如 `isAdmin`, `balance`, `role` 等）。
  - **教学价值**: 讲解DTO（数据传输对象）模式的重要性，即API的输入和输出应使用专门的DTO对象，仅包含必要且允许客户端读写的字段。
  - **实现场景**: 创建一个“更新用户信息”的API `/api/v1/user/update`，漏洞版本直接将传入的JSON绑定到 `User` 实体并保存，允许攻击者传入 `{"username":"new_name", "isAdmin":true}` 来提升自己的权限。

- **建议2：资源缺失和速率限制不足 (Lack of Resources & Rate Limiting)**
  - **漏洞描述**: 关键API端点（如登录、发送短信验证码、重置密码）没有频率限制，允许攻击者进行暴力破解、短信轰炸或拒绝服务攻击。
  - **教学价值**: 介绍API网关或在应用层面使用拦截器/过滤器实现速率限制的必要性，保护系统资源和用户账户安全。
  - **实现场景**: 创建一个发送短信验证码的接口 `/api/v1/auth/send-sms-code`。漏洞版本可以被无限制调用。安全版本则会限制同一手机号或同一IP在单位时间内的请求次数。

---

## 3. 安全日志记录和监控失败 (Security Logging and Monitoring Failures)

这是OWASP Top 10:2021新增的重要类别，强调“可检测性”也是安全的一部分。

- **建议1：关键安全事件缺失日志 (Missing Logs for Security Events)**
  - **漏洞描述**: 系统在发生登录失败、权限变更、密码重置、访问拒绝（403）等关键安全事件时，没有生成任何日志。
  - **教学价值**: 强调日志不仅用于排查故障，更是安全事件追溯和入侵检测的唯一依据。教育开发者哪些事件是必须记录的。
  - **实现场景**: 在“登录”接口中，如果验证失败，漏洞版本仅返回错误信息而无任何日志记录。安全版本则会记录一条包含时间、用户名、源IP的登录失败日志。

- **建议2：日志内容不规范 (Inadequate Log Content)**
  - **漏洞描述**: 日志虽然记录了，但信息不足，无法用于有效的安全分析。例如，仅记录“登录失败”，但没有记录是哪个用户、哪个IP尝试的。
  - **教学价值**: 讲解一条有效的安全日志应包含哪些元素（Who, What, When, Where, Result），以及如何确保日志的完整性和不可否认性。
  - **实现场景**: 在一个需要管理员权限的接口，当普通用户访问时，漏洞版本仅记录“Access Denied”。安全版本则会记录“User 'user1' (ID: 123) from IP 192.168.1.10 tried to access admin resource '/api/v1/admin/dashboard' and was denied.”

---

## 4. 补充常见Web漏洞

这些是您现有文件结构中已包含但未在`VULNERABILITIES.md`中明确列出的常见漏洞，建议将其正式化并创建教学案例。

- **建议1：开放重定向 (Open Redirect)**
  - **漏洞描述**: 应用程序将用户重定向到由参数指定的URL，但未对该URL进行验证，导致攻击者可以构造链接，将用户重定向到恶意网站。
  - **教学价值**: 常用于钓鱼攻击。教育开发者必须对所有重定向目标地址进行白名单验证。
  - **实现场景**: 创建一个登出或跳转接口 `GET /logout?redirect_url=http://malicious.com`。

- **建议2：点击劫持 (Clickjacking)**
  - **漏洞描述**: 缺少 `X-Frame-Options` 或 `Content-Security-Policy: frame-ancestors` 响应头，导致网页可被恶意网站通过 `<iframe>` 嵌入，诱使用户在不知情的情况下点击页面上的按钮。
  - **教学价值**: 这是防御UI层面攻击的重要一环，讲解如何通过HTTP响应头来缓解。
  - **实现场景**: 创建一个简单的“修改密码”页面，漏洞版本无任何相关响应头。安全版本则添加 `X-Frame-Options: DENY` 头。

- **建议3：HTTP请求走私 (HTTP Request Smuggling)**
  - **漏洞描述**: 当架构中存在代理服务器（如Nginx）和后端服务器时，利用两者对HTTP请求解析的差异，构造一个能“走私”第二个请求的恶意请求，从而绕过前端代理的安全控制。
  - **教学价值**: 这是一个更高级的网络层攻击，有助于理解HTTP协议的复杂性和反向代理环境下的安全风险。
  - **实现场景**: 这需要一个稍微复杂的环境（例如，用Docker Compose启动一个Nginx和一个Spring Boot应用），通过构造包含 `Content-Length` 和 `Transfer-Encoding: chunked` 的请求来演示。
