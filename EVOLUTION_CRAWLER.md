# 技术演进计划：从终端 AI 编程助手 → 百亿级 URL 分布式爬虫与开源情报（OSINT）平台

> 面向中国开发者。说人话，分步走，每步都可独立验收。
> 本计划**只动底层基础设施与数据管道**，TUI / CLI / HTTP 三大用户交互入口的行为与用法保持不变（符合"改造只影响底层"约束）。
> 最后更新：2026-08-06

---

## 0. 一句话愿景

今天 mimofan 是一个"**把自然语言转成工具调用**"的本地智能体运行时。
明天它要变成"**能管理百亿级 URL、持续监测开源情报、并把多模态网页数据清洗/去重/标准化/结构化**"的分布式系统。

二者并不矛盾——**核心能力（调 LLM 思考 + 调度工具 + 管理会话/任务）完全复用**，只是"工具"从"读本地文件"扩展成"爬全网 + 解析多模态"，"状态"从"单机 SQLite"扩展成"分布式存储"。

---

## 1. 现状能力盘点（哪些能直接复用）

| 现有能力 | 文件位置 | 演进后怎么用 |
|---------|---------|------------|
| 工具调度框架 `ToolHandler` / `ToolRegistry` | `crates/tools/src/lib.rs:384` | 爬虫 = 一类新工具（fetcher / parser） |
| 并行/串行门禁 `ToolCallRuntime` | `crates/tools/src/lib.rs:417` | 控制并发抓取速率，防被封 |
| 任务管理 `JobManager` | `crates/core/src/job.rs:107` | 分布式抓取任务编排 |
| 会话/线程模型 `ThreadManager` | `crates/core/src/thread.rs:52` | 一次"情报调研任务"= 一个 thread |
| 执行策略 `ExecPolicyEngine` | `crates/execpolicy/src/lib.rs` | 爬取合规/频率/域名白名单门禁 |
| 模型路由 `model_routing.rs` | `crates/tui/src/model_routing.rs` | 轻任务用便宜模型，解析用强模型 |
| 提示词分层宪法 | `crates/tui/src/prompts/` | 情报抽取 prompt 直接挂到分层体系 |
| 协议 DTO `protocol` | `crates/protocol/src/` | 集群节点间通信复用同一套类型 |
| HTTP 服务 `app-server` | `crates/app-server/src/lib.rs` | 对外暴露"情报查询 API" |

**结论**：业务编排层（Domain）几乎零改动，主要工作量在**新增数据管道 crate + 替换存储后端 + 加调度层**。

---

## 2. 目标架构（演进后长这样）

```
┌──────────────────────────────────────────────────────────────────┐
│                        用户交互层（不变）                            │
│   TUI 终端 │ CLI 命令行 │ HTTP API(app-server)  ← 用法完全不变      │
└───────────────┬──────────────────────────────────────────────────┘
                │  复用现有 Runtime / ThreadManager / JobManager
                ▼
┌──────────────────────────────────────────────────────────────────┐
│                      编排层（基本复用 + 扩展）                       │
│  CrawlOrchestrator（新） 调度 URL 队列 / 去重 / 频率控制            │
│  IntelligenceAgent（新） 把"调研任务"翻译成抓取+解析计划            │
└───────────────┬──────────────────────────────────────────────────┘
                │
   ┌────────────┼────────────────────────────────────────────┐
   ▼            ▼            ▼            ▼          ▼         ▼
┌────────┐ ┌────────┐ ┌──────────┐ ┌────────┐ ┌────────┐ ┌───────┐
│Fetcher │ │Parser  │ │Multimodal│ │Dedup   │ │Storage │ │Policy │
│ 抓取工具│ │网页解析│ │清洗标准化│ │ 去重    │ │分片存储│ │合规门禁│
│(新crate)│ │(新crate)│(新crate) │ │(新crate)│ │(新crate)│ │(复用) │
└────────┘ └────────┘ └──────────┘ └────────┘ └────────┘ └───────┘
                │            │            │           │
                └────────────┴────────────┴───────────┘
                ▼
┌──────────────────────────────────────────────────────────────────┐
│                   分布式基础设施层（全新增）                         │
│  URL 队列(Kafka/NATS) │ 对象存储(S3/MinIO) │ 分布式KV(TiKV/Redis) │
│  倒排索引(Elasticsearch) │ 向量库(已备 memory crate 可改造)       │
└──────────────────────────────────────────────────────────────────┘
```

**关键红线**（来自 `ARCHITECTURE_STABILITY.md` §7）：
- 绝不在集群里用"单 `Mutex<Runtime>`"模式 → 节点必须**无状态**，状态外置。
- SQLite 只做本地元数据 → 百亿 URL 必须**分片 + 倒排索引**。

---

## 3. 分步演进路线（每步可独立交付）

### 阶段 0：夯实底座（1–2 周，低风险）

目标：把 `ARCHITECTURE_STABILITY.md` §1 的 app-server 并发问题修掉，并抽象出"可插拔存储"接口，为分布式铺路。

- [ ] 把 `app-server` 的 `Arc<Mutex<Runtime>>` 拆成读写锁 + 任务通道（见稳定性文档 §1 方案 A/B）。
- [ ] 在 `crates/state` 抽象 `StateRepository` trait（当前 `StateStore` 是 SQLite 直连），让未来可换成分布式实现。
- [ ] `Runtime` 改为持有 `Arc<dyn StateRepository>`，而非具体 SQLite。
- [ ] 验收：`cargo test --workspace` 通过；app-server 并发压测无队头阻塞。

> 这一步**完全不动**用户交互层，只是把存储从"具体类"换成"接口"。

### 阶段 1：把"爬一个 URL"做成一种工具（2–3 周）

目标：验证"爬虫 = 工具"的复用假设，单机可跑。

- [ ] 新增 `crates/fetcher`：实现 `ToolHandler`，输入 URL → 输出原始字节（复用 `reqwest`，已在工作区）。
- [ ] 频率/并发控制复用 `ToolCallRuntime` 的读写锁模型（串行爬取 = 写锁，并行不同域 = 读锁）。
- [ ] 合规门禁复用 `ExecPolicyEngine`（把"禁止爬取的域名"做成 deny 规则）。
- [ ] 在 `prompts/` 加 `intel/crawl_plan.md`，让 IntelligenceAgent 把"调研 X 公司"翻译成"先抓官网、再抓新闻、再抓招聘"。
- [ ] 验收：TUI 里 `@crawl https://example.com` 能返回内容；频率超限被策略拦下。

### 阶段 2：网页解析与结构化抽取（2–3 周）

目标：把原始 HTML 变成结构化数据。

- [ ] 新增 `crates/parser`：HTML→正文/标题/发布时间/作者（readability 类算法）。
- [ ] 用现有 LLM 路由：强模型做"非结构化→JSON"抽取（如抽取"融资事件"字段），便宜模型做"是否相关"二分类。
- [ ] 抽取结果写入 `StateRepository`（暂仍 SQLite，但接口已抽象）。
- [ ] 验收：给定一篇新闻，自动抽出 `{公司, 金额, 轮次, 日期}` JSON。

### 阶段 3：多模态清洗 / 去重 / 标准化（3–4 周）

目标：图片、PDF、视频字幕也能进管道。

- [ ] 新增 `crates/multimodal`：
  - **清洗**：图片去水印/尺寸归一、PDF 转文本、视频 ASR 取字幕。
  - **标准化**：统一成 `{source_url, content_type, text, embedding, captured_at, lang}` 规范 schema。
  - **结构化解析**：表格/公式/代码块识别。
- [ ] 新增 `crates/dedup`：
  - **精确去重**：URL 规范化（去追踪参数）+ 内容哈希（sha2，已在工作区）。
  - **语义去重**：复用 `crates/memory` 的 embedding + hnsw（**这正是 memory crate 的归宿**，不再是僵尸上下文）。
- [ ] 验收：同一文章的不同镜像 URL 被判为重复；改标题不改正文的"伪原创"被语义去重捕获。

### 阶段 4：URL 管理与调度（百亿级）（4–6 周）

目标：从"爬一个"到"管理百亿 URL 的生命周期"。

- [ ] 新增 `crates/crawl-scheduler`：
  - URL 队列：用 Kafka / NATS（消息队列）做待抓队列，天然支持分布式消费。
  - **分片**：URL 按域名哈希分片，每片独立存储与调度（解决单点）。
  - **优先级**：新闻类高优、论坛类低优；按 `reasoning_effort` 类似思路做"抓取力度"。
  - **去重 Frontier**：已抓 URL 布隆过滤器（Bloom Filter）快速判重，避免重复入队。
- [ ] 存储替换：`StateRepository` 实现分布式版——对象存储存原始内容，分布式 KV 存 URL 状态，Elasticsearch 存倒排索引供检索。
- [ ] 验收：压测 10^8 级 URL 入队/出队，单机吞吐达标；布隆过滤器误判率 < 1%。

### 阶段 5：开源情报监测（持续化）（4–6 周）

目标：从"按需爬"到"持续监测 + 告警"。

- [ ] 监测规则引擎：用户声明"监测某关键词/实体"，系统周期性调度相关 URL 抓取。
- [ ] 变化检测：对同一 URL 的历史快照做 diff（复用 `compact.md` 思路），发现变更即触发 IntelligenceAgent 小结。
- [ ] 告警出口：复用 `crates/hooks`（已有 Stdout/Jsonl/UnixSocket sink）→ 推送到 Slack/Webhook/数据库。
- [ ] 情报图谱（可选）：实体-关系抽取，存入图数据库。
- [ ] 验收：声明"监测竞品 A 融资"，下次 A 发融资新闻时自动收到结构化摘要。

### 阶段 6：分布式集群化（6–8 周）

目标：多节点横向扩展，百亿级常态运行。

- [ ] 节点无状态化：每个抓取/解析节点不持有共享状态，状态全在阶段 0 的 `StateRepository` 分布式实现里。
- [ ] 服务发现 + 负载均衡：K8s Deployment + Service，或 Consul/Nacos。
- [ ] 分布式锁：URL 调度用 Redis/etcd 锁替代任何 `std::sync::Mutex`（呼应稳定性红线）。
- [ ] 可观测性：复用 `tracing` 体系，加 Prometheus 指标（抓取成功率、去重率、队列深度）。
- [ ] 验收：10 节点集群跑满，单节点宕机不影响整体（任务自动重调度）。

---

## 4. 演进过程中"绝对不动"的边界

| 保持不变 | 原因 |
|---------|------|
| TUI / CLI / HTTP 三大入口的用法 | 用户约束：改造只影响底层 |
| `ToolHandler` / `ToolRegistry` 抽象 | 爬虫只是新工具，接口复用 |
| 提示词分层宪法体系 | 情报抽取 prompt 挂进去即可 |
| `ExecPolicyEngine` 审批模型 | 爬取合规直接复用 |
| `protocol` DTO 类型 | 集群通信复用，避免重新定义 |
| `ToolCallRuntime` 并发模型 | 已被验证正确（见稳定性文档 §2） |

---

## 5. 风险与缓解

| 风险 | 阶段 | 缓解 |
|------|------|------|
| 分布式存储改造破坏现有 SQLite 用户 | 0 | trait 抽象 + 默认仍 SQLite，灰度切换 |
| 爬虫被目标站封禁 | 1–4 | 复用 ExecPolicy + 频率门禁 + 代理池 |
| 百亿 URL 存储成本爆炸 | 4 | 分片 + 冷热分离（对象存储低频层） |
| 语义去重误杀 | 3 | 阈值可调 + 人工复核接口 |
| 情报合规/法律风险 | 5 | 域名白名单 + robots.txt 尊重 + 审计日志 |
| 集群单点 | 6 | 无状态节点 + 外部状态存储 + 重调度 |

---

## 6. 与现有文档的关系

- `ARCHITECTURE_CN.md`：当前架构说明（演进起点）。
- `ARCHITECTURE_IMPROVEMENT_PLAN.md`：DDD 存量优化（阶段 0 的底座在此）。
- `ARCHITECTURE_STABILITY.md`：稳定性红线（阶段 0/6 必须遵守）。
- 本文档：未来演进路线（阶段 1–6）。

> 不画大饼：阶段 0–1 是低风险、可立刻开工的；阶段 4–6 需要引入消息队列/分布式存储等新基建，应单独立项评估，不在本次强行铺开。
