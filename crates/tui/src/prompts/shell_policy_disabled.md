Shell tools unavailable. For mandatory-use items referencing `exec_shell`, use `code_execution` (Python sandbox). For GitHub triage, use `github_issue_context` / `github_pr_context` as primary route.
