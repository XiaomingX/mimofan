{
  "name": "@mimofan/feishu-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Feishu/Lark mobile bridge for a local mimofan serve --http runtime.",
  "main": "src/index.mjs",
  "scripts": {
    "start": "bun src/index.mjs",
    "check": "bun --check src/index.mjs && bun --check src/lib.mjs && bun --check scripts/validate-config.mjs",
    "test": "bun test test/*.test.mjs",
    "validate:config": "bun scripts/validate-config.mjs"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.52.0"
  },
  "engines": {
    "bun": ">=1.0"
  }
}
