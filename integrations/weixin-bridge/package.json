{
  "name": "@mimofan/weixin-bot-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Weixin iLink Bot bridge for a local mimofan serve --http runtime. Scan QR to login.",
  "main": "src/index.mjs",
  "scripts": {
    "start": "bun src/index.mjs",
    "check": "bun --check src/index.mjs && bun --check src/lib.mjs",
    "test": "bun test test/*.test.mjs"
  },
  "engines": {
    "bun": ">=1.0"
  }
}
