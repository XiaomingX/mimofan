{
  "name": "@mimofan/bridge-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared pure helpers for Mimofan chat bridges.",
  "main": "src/lib.mjs",
  "scripts": {
    "check": "bun --check src/lib.mjs",
    "test": "bun test test/*.test.mjs"
  },
  "engines": {
    "bun": ">=1.0"
  }
}
